# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# A library lives outside the repository (see `tabless where`). If someone
# points TABLESS_HOME at a checkout, this keeps their documents out of git.
_index/
_inbox/
projects.toml
.cache/

# Machine-specific agent notes; CLAUDE.md is the committed, public one.
CLAUDE.local.md

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
