"""tabless -- a local shelf and reader for the HTML your coding agent writes.

Reports, prototypes, docs and experiment write-ups get filed by project and by
type, kept as durable copies, and read in a single window that never becomes
another browser tab.

Submodules are imported lazily by the CLI; importing this package on its own is
cheap and has no side effects beyond resolving paths.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
